<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.pktech.dao.UserDao">


    <resultMap id="BaseResultMap" type="com.pktech.entity.User">
        <id column="id" property="id" jdbcType="VARCHAR"/>
        <result column="username" property="username" jdbcType="VARCHAR"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="password" property="password" jdbcType="VARCHAR"/>
        <result column="phone" property="phone" jdbcType="DATE"/>
       
    </resultMap>
    <sql id="Base_Column_List">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        id, username, name,password,phone
    </sql>
    <select id="findUserByPhone" parameterType="java.lang.String" resultType="com.pktech.entity.User" >
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        select
        <include refid="Base_Column_List"/>
        from usertest
        where phone=#{phone}
    </select>
    <insert id="insert" parameterType="com.pktech.entity.User">
   		insert into usertest(
   			username,name,password,phone
   		) values(
   			#{username},#{name},#{password},#{phone}
   		);
   		
    </insert>
    
    <!-- 通过用户名查询 -->
	<!-- User getUserByUsername(String username); -->
	<select id="getUserByUsername" 
		resultType="com.pktech.entity.User">
		
		select 
			id,username,name,password,
			phone
		from 
			usertest
		where 
			username=#{username};
	</select>
	
<!-- 	<select id="getCountByName" 
		resultType="java.lang.Integer">
		select 
			count(name)
		from 
			usertest
		where 
			name=#{name}
		
	</select>
	
	<select id="getCountByPhone" 
		resultType="java.lang.Integer">
		select 
			count(phone)
		from 
			usertest
		where 
			phone=#{phone}
		
	</select>
     -->
    
    
</mapper>